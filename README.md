# Learning project for small things

Currently, this service only used to try registering a service in Eureka server. 
To run multiple instances of the service we need to run them in separate terminals. 